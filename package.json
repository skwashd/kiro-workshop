{
  "name": "pulse-game",
  "version": "1.0.0",
  "description": "Pulse — a minimalist timing game with persistent high scores",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "npm install --production"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "express": "^4.21.0",
    "cors": "^2.8.5"
  }
}
